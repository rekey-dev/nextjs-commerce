# Panel -> your Application -> Developer -> API keys.

# Server-only. Grants full API access for this Application. Never commit it.
REKEY_SECRET=rp_live_replace_me
REKEY_URL=https://api.rekey.dev

# Safe in the browser. Identifies the Application; grants nothing on its own.
NEXT_PUBLIC_REKEY_PUBLIC_KEY=rp_pub_replace_me
NEXT_PUBLIC_REKEY_URL=https://api.rekey.dev

# Where this shop is reachable. Checkout returns the buyer here, so in
# production it has to be the real origin.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# The catalogue file. Anywhere writable; keep it off the repo.
DATABASE_FILE=./shop.db
